# 动作库 App 打包说明

## 你需要准备的软件（在你自己的电脑上装，不是在这个对话里）

1. **Node.js**（18 或更高版本）— https://nodejs.org 下载 LTS 版即可
2. **Android Studio**（最新版）— https://developer.android.com/studio
   安装时选默认选项，它会自动带上 Android SDK
3. 一张 **1024×1024 的 PNG 图片**作为 App 图标（可选，没有的话用默认图标也能跑）

---

## 第一步：整理文件夹

1. 新建一个空文件夹，比如叫 `exercise-app`
2. 把这次给你的 3 个配置文件（`package.json`、`capacitor.config.json`、本说明）放进这个文件夹根目录
3. 在这个文件夹里再新建一个子文件夹，命名为 **`www`**
4. 把你之前下载解压好的整个项目内容（`index.html`——记得用我之前给你的中文版、`images/`、`videos/`、`data/`、`setup.html`）全部复制进 `www` 文件夹里

整理完的结构应该长这样：

```
exercise-app/
├── package.json
├── capacitor.config.json
└── www/
    ├── index.html
    ├── setup.html
    ├── images/
    ├── videos/
    └── data/
```

---

## 第二步：打开终端，安装依赖

在 `exercise-app` 文件夹里打开终端（Mac 用"终端"，Windows 用 PowerShell 或 CMD），依次执行：

```bash
npm install
npx cap add android
npx cap sync
```

- 第一行：安装 Capacitor 相关的包
- 第二行：生成一个完整的 Android 原生项目（会新建一个 `android/` 文件夹）
- 第三行：把 `www` 里的内容同步进安卓项目

如果这一步网络较慢，多等一会儿或换个网络环境重试。

---

## 第三步：用 Android Studio 打开并编译

```bash
npx cap open android
```

这条命令会自动打开 Android Studio 并加载项目。第一次打开可能需要几分钟让它做 Gradle 同步（右下角会有进度条），等它跑完再进行下一步。

**编译测试版 APK（不需要签名，可以直接装手机测试）：**

菜单栏 → `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`

编译完成后会弹出提示，点击 `locate` 就能找到文件，路径大概是：

```
exercise-app/android/app/build/outputs/apk/debug/app-debug.apk
```

把这个 `.apk` 文件传到手机上（微信传文件、数据线拷贝、网盘都行），点开安装即可。手机可能会提示"未知来源应用"，允许安装就行。

---

## 第四步（可选）：换 App 图标和名字

- **改名字**：改 `capacitor.config.json` 里的 `appName` 字段，改完重新跑一遍 `npx cap sync`
- **换图标**：在 Android Studio 里右键 `app` 文件夹 → `New` → `Image Asset`，按提示选你的 1024×1024 图片，它会自动生成各种分辨率

---

## 第五步（可选，正式使用才需要）：签名打包

上面生成的 `app-debug.apk` 只能自己测试用，不能长期稳定使用（每次重装都要覆盖，也没法上传应用商店）。如果想要一个"正式版"：

菜单栏 → `Build` → `Generate Signed Bundle / APK` → 选 `APK` → 点 `Create new...` 新建一个签名密钥（keystore）

**重要：这个 keystore 文件和你设的密码一定要备份好**，以后每次更新这个 App 都要用同一个 keystore，丢了就没法再更新，只能换个新包名重新发布。

---

## 常见问题

- **`npx cap add android` 报错找不到 Android SDK**：说明 Android Studio 装好后没有把 SDK 路径配置好，打开 Android Studio → `Settings` → `Languages & Frameworks` → `Android SDK`，确认路径存在，通常会自动配好环境变量，重启终端再试一次
- **图片加载不出来**：检查 `www/images` 和 `www/videos` 文件夹是否真的完整复制进去了，路径大小写要完全对应
- **App 图标还是默认的机器人图标**：图标资源需要重新生成后再跑一次 `npx cap sync`，Android Studio 也可能需要 `Build → Clean Project` 后重新编译才会刷新
